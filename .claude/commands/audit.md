Your goal it to update any vulenrable dependencies.

Do the following:

1. Run 'npm audit' to vulnerable installed packages in the project
2. Run 'npm audit fix' to apply updates
3. Run tests and verify the udpates dind't break anything